# Python 版本要求
# Python 3.12.10 或更高版本（建议使用 Python 3.12.x）

# 基础依赖
openpyxl>=3.1.2
pandas>=2.0.0
xlrd==1.2.0  # 使用1.2.0版本以支持旧的.xls格式
xlwt>=1.3.0  # 用于写入旧的.xls格式文件
xlutils>=2.0.0  # 用于复制和修改.xls文件
edge-tts>=6.1.0
numpy>=1.24.0
torch>=2.0.0
pyinstaller>=5.13.0
pypinyin>=0.50.0  # 用于姓名拼音转换和模糊匹配

# 语音识别依赖（需要先安装）
# tokenizers 已安装: 0.22.2
# 然后安装: pip install av faster-whisper ctranslate2
faster-whisper>=1.2.0
onnxruntime>=1.16.0  # VAD过滤器所需（提高识别准确率）

# 音频输入（优先使用sounddevice，更易安装）
sounddevice>=0.4.6
scipy>=1.10.0
# 备用方案（如果sounddevice不可用）
# pyaudio>=0.2.14
